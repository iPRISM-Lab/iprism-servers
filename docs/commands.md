# Useful Commands

```bash
# Check NVIDIA GPU usage
nvidia-smi

# Check AMD GPU usage
rockm-smi

# Check open ports
sudo netstat -tulnp

# Docker containers
docker ps

# Disk usage
df -h

# Active processes
htop
```
